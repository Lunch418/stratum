# Сверка с оригиналом: что из Stratum 2000 есть в Stratum Modern

Источник по оригиналу — ресурсы `r1251s32.dll` (18 меню и 107 диалогов,
сняты `tools/dump_resources.py`, см. `docs/original/`), справка `SC3.HLP`
(1329 тем) и таблицы компилятора (960 функций). Главное меню новой среды
повторяет структуру оригинала пункт в пункт (`app/src/menus.tsx`); пункты,
которым нет места, показаны выключенными с объяснением в подсказке. Статусы: **есть** — сделано в новом
интерфейсе; **иначе** — то же назначение, но другим способом; **частично**;
**нет**; **не нужно** — свойство MDI/Windows 98, у которого в современной среде
нет смысла.

## Функции языка (960)

| Группа | Всего | Статус |
| --- | --- | --- |
| Математика, строки, матрицы, массивы, потоки, файлы, системные | 331 | есть (потоки FILE/MEMORY; `MessageBox` и `InputBox` — настоящие окна: такт откатывается, ждёт ответа и повторяется с ним; файловые диалоги и выбор цвета — тоже окна с обзором папок ядра) |
| 2D-графика и окна | ~215 | есть, включая слои (`SetObjectLayer2d`, `SetSpaceLayers2d`); видео-кадры (`CreateVideoFrame2d`, `VideoPlay2d`…) — заглушки |
| Интерфейсные элементы (контролы, LB*) | 21 | есть |
| Имиджи и схема (`GetObjectCount`, `GetVarInfo`, `SetModelText`…) | 25 | есть, кроме перестройки схемы на ходу: `CreateObject`, `DeleteObject`, `CreateLink`, `RemoveLink`, `SetLinkVars`, `CreateClass`, `DeleteClass`, `SetCalcOrder`, `Load/SaveObjectState`, `LoadProject` — принимаются без действия и попадают в список «не реализовано» |
| 3D (`*3d`, `3DTOOLS.TDL`) | ~50 | есть, кроме текстур и `CreateObjectFromFile3d` (v3d), `SweepAndExtrude3d` |
| Базы данных DBF (`Db*`) | 49 | заглушки (нет BDE) |
| Анализатор текста | 10 | заглушки |
| Ogre3D (`Root_*`, `Entity_*`, `SceneNode_*`…) | 315 | нет — отдельный движок-плагин; вызовы возвращают 0 и считаются |
| NUI (Kinect), USB-ключи, SMS/почта | 12 | заглушки |

Проверка: `python3 tools/check_functions.py` печатает функции таблиц, для
которых в ядре нет ветки.

## Главное меню

### Файл
| Оригинал | Статус | Где |
| --- | --- | --- |
| Новый (проект / имидж / рисунок) | есть | «Проект ▾ → Новый проект…» (папка, корневой имидж); имидж — «+ новый»; рисунок — вкладка «Рисунок» |
| Открыть | есть | Ctrl+O, диалог с обзором папок и недавними; `.spj`, `.prj`, `project.json` |
| Сохранить / Сохранить как / Сохранить проект как / Сохранить все | есть | Ctrl+S, «Сохранить как…» — родной формат; «Экспорт…» — `.spj/.cls/.stt` |
| Печатать, Параметры печати | есть | Ctrl+P — печать активной вкладки средствами браузера; параметров печати нет |
| Список закрытых проектов | есть | «Недавние» в диалоге открытия |
| Выход | есть | закрытие окна; предупреждение о несохранённых правках |

### Правка
| Оригинал | Статус | Где |
| --- | --- | --- |
| Отмена / Повтор | есть | Ctrl+Z / Ctrl+Shift+Z, 100 шагов, охватывают текст, переменные, схему, связи |
| Вырезать / Копировать / Вставить / Дублировать (имиджи на схеме) | есть | Ctrl+X/C/V/D и контекстное меню блока |
| Удалить | есть | Delete на схеме |
| Поиск / Искать снова | есть | Ctrl+F в тексте; Ctrl+Shift+F — панель «Поиск» по текстам, переменным и именам имиджей с переходом к строке |

### Вид
| Оригинал | Статус | Где |
| --- | --- | --- |
| Панели инструментов, строка состояния, главное меню | иначе | одна верхняя панель + строка состояния; панели не отключаются |
| Главная схема | есть | вкладка «Схема», крошки пути |
| Библиотеки | есть | раздел «Библиотеки» в иерархии, перетаскивание на схему |
| Имиджи | есть | раздел «Имиджи проекта» |
| Инспектор объектов | есть | правая колонка (в оригинале «не реализовано») |
| Сообщения | есть | нижняя панель, фильтр |
| Выполнить | есть | вкладка «Окно модели» |
| Иерархия проекта | есть | левая колонка |
| Просмотр переменных (Watch) | есть | инспектор: живые значения, правка на паузе, наблюдение выражений, графики |
| Информация | есть | «Проект ▾ → Информация…»: имиджи, экземпляры, связи, уравнения, функции с частотой, свойства, библиотеки |

### Вставка
| Оригинал | Статус | Где |
| --- | --- | --- |
| Новый двухмерный объект (линия, полилиния, прямоугольник, эллипс, дуга, текст) | есть | вкладки «Рисунок» и «Иконка» — панель инструментов над листом |
| Формы, 3d | есть / нет | «Вставка → Формы» ставит Windows-элемент (Edit, ComboBox, CheckBox, RadioButton, Button, ListBox) на лист; камеры 3d — только из кода |
| Имидж | есть | перетаскивание из иерархии |
| Связь | есть | тяга от порта к блоку |
| Создать и вставить новый имидж | частично | «+ новый», затем перетащить |
| Контактная площадка | нет | |
| OLE объект | не нужно | |
| Из файла | есть | «Вставка → Из файла…» и кнопка на панели рисования: `.vdr` (как группа) и `.bmp` |

### Формат
| Оригинал | Статус | Где |
| --- | --- | --- |
| Z-порядок, Слои, Актуальные размеры | есть | Z-порядок: PgUp/PgDn, Ctrl+PgUp/PgDn в редакторе рисунка; слои схемы Alt+1…4; слои листа 0–31 — «Параметры листа» |
| Параметры (имиджа / объекта / связи) | есть | «Свойства имиджа…» (Enter): имя, примечания, флаги «Дополнительно»; объект окна модели: Alt+щелчок; связь: двойной щелчок или контекстное меню — пары и «Дополнительно» (цвет, толщина, выключение, стрелки, слой) |
| Добавить в группу / Разгруппировать | есть | кнопки в редакторе рисунка |
| Редактировать схему | есть | двойной щелчок по блоку |
| Исходный текст | есть | вкладка «Код» |
| Изображение | есть | вкладка «Рисунок» (лист общий с схемой, элементы схемы скрыты) |
| Параметры листа (фон, координаты, окно, слои, инструменты, объекты) | есть | сетка (начало, шаг, видимость, привязка), окно (стиль, размер, прокрутка), слои 0–31, «запретить подокна»; хранятся в родном формате |
| Редактировать порядок вычислений | есть | диалог со списком блоков и кнопками выше/ниже |

### Моделирование
| Оригинал | Статус | Где |
| --- | --- | --- |
| Запуск / Один шаг / Пауза | есть | F5 / F10 / F5; плюс «Назад» (Shift+F10) |
| Очистить / По умолчанию / Установить по умолчанию | есть | «Сброс» — пересборка; «Модель ▾ → Переменные по умолчанию» |
| Установить переменные | есть | инспектор на паузе |
| Сохранить / Загрузить переменные (`.stt`) | есть | «Модель ▾ → Сохранить/Загрузить состояние в .stt…» |
| Сохранить / Загрузить из DEFAULT.STT | иначе | «Запомнить как стартовое состояние» пишет state.json/_preload.stt проекта; старая редакция DEFAULT.STT не разобрана |
| Сохранить / Прочитать переменные имиджа | есть | `.stt` только с экземплярами выбранного имиджа |

### Параметры
| Оригинал | Статус |
| --- | --- |
| Параметры среды (2D-редактор, вычисления, редактор, язык, библиотеки, загрузка, методы, панели, пользователь, переменные) | частично — опрос окна, автосохранение, точек в окружности, шрифт, тема, раскладка, список библиотек; язык и загрузка модулей не нужны |
| Параметры проекта (переменные, библиотека, методы, вычисления, инфо) | есть — вычисления (режим, таймер, тактов за цикл, обработка математических ошибок `MathMode`: стоп / предупреждение / протокол / не замечать), методы (итерации и точность Ньютона), переменные (`vars_logset`, `vars_preload`), информация об авторе; ядро применяет их сразу |
| Инструменты | не нужно |

### Окна
Каскадом, черепицей, упорядочить иконки, закрыть все, список окон — **не нужно**:
вместо MDI — фиксированная раскладка панелей с разделителями.

### Помощь
Справка по темам и по функции под курсором — **есть** (панель «Справка», F1).

### Контекстные меню
Схема: блок — открыть код, переименовать, копировать, дублировать, заменить
другим…, удалить; лист — параметры листа, порядок вычислений, свойства
имиджа, выстроить по узлам сетки, вставить; связь — свойства, удалить.
Иерархия: свойства имиджа, текст, схема, изображение, граф, новый имидж,
удалить. Нет: «конвертировать в один имидж».

## Панели инструментов

### Стандартная
Новый проект (частично), Открыть (есть), Печать (нет), Удалить (есть),
Вырезать/Копировать/Вставить (есть на схеме), Записать/Загрузить состояние (есть),
Записать все (есть), Записать активное окно в VDR (нет), Информация о системе
(есть), Иерархия/Библиотеки/Сообщения (есть).

### Управление
Пауза, Запуск, Шаг, Сброс — есть; предыдущая/следующая страница (гиперпереходы)
— нет.

### Рисование — есть (вкладки «Рисунок», «Иконка»)
Режим выбора и перемещение с ручками, линия, полилиния, прямоугольник,
скруглённый прямоугольник, эллипс, дуга, текст, группа/разгруппировать,
Z-порядок, правка точек (Ctrl — добавить, Shift — удалить), панорама, зум,
размер листа, цвет/толщина линии и заливка; правки идут в Undo/Redo и в файл
`.vdr`. Вставка из файла и порядок вычислений — есть.

### Битовый редактор — есть
Кнопка «Битовый редактор» на панели рисования для выбранного растра (и «Новая
битовая карта»): карандаш, ластик, линия, прямоугольник, закрашенный
прямоугольник, эллипс, закраска, распылитель, пипетка, цвет, сетка, зум,
отмена, ножницы (обрезка до прямоугольника); результат пишется в растр
рисунка (`.vdr`).

### Видео, Базы данных — не нужно / нет
### 3D-графика, Трёхмерные примитивы — нет (есть только функции языка)

## Диалоги и окна

| Оригинал | Статус |
| --- | --- |
| Параметры двухмерного объекта: Положение, Линия, Заливка, Текст, Гипербаза, Информация, Видео, BMP, Группа, Проекция 3D, Материалы, Точки, Переменные, Windows-элементы | частично — Положение, Линия, Заливка, Точки, Текст (чтение), Контрол (чтение) в инспекторе |
| Параметры связи: Основное, Дополнительно | есть — пары переменных; цвет, толщина, выключение, стрелки, слой |
| Параметры листа: Фон, Координаты, Окно, Разное, Слои, Инструменты, Объекты | есть — Сетка, Окно (стиль и размер применяются к окну модели), Слои, Разное; фон/инструменты/объекты — в редакторе рисунка |
| Параметры проекта, Параметры среды | есть / частично |
| Информация (уравнения, функции, разное) | есть |
| Информация о переменной (информация, используемость) | есть — подсказка у порта; двойной щелчок по имени в инспекторе открывает «где используется» в панели «Поиск» |
| Выбор имиджа | иначе — перетаскивание из иерархии |
| Создание проекта (имя файла, путь, корневой имидж) | есть |
| Дополнительные свойства имиджа | есть — флаги во вкладке «Дополнительно» свойств имиджа (биты сопоставлены по корпусу) |
| Удаление имиджей (список с числом экземпляров) | есть — «Правка → Удаление имиджей…»; неиспользуемые отмечены заранее |
| Новый имидж / иконка имиджа (библиотека иконок `.dbm`) | есть — вкладка «Иконка» в свойствах имиджа: наборы иконок рядом с библиотеками, выбор ячейки, сброс |
| О программе | есть |
| Запись изменений | есть — предупреждение при закрытии, автосохранение |
| Выбор масштаба | иначе — колесо и «100 %» |
| Поиск | частично — Monaco |
| Окно редактирования текста | есть — Monaco с грамматикой, подсказками, ошибками |
| Окно просмотра переменных | есть |
| Окно библиотек, Окно иерархии, Окно сообщений | есть |
| Matrix editor (просмотр и правка матриц) | есть — панель «Отладка → Матрицы» |
| Input Box, MessageBox модели | есть — модальные окна поверх окна модели |
| Input Box, MessageBox модели | есть — модальные окна поверх окна модели |
| Окно просмотра базы данных | нет |

## Что делается дальше (по убыванию важности)

1. Контактные площадки; параметры печати; «конвертировать в один имидж».
