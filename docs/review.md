# Ревью безопасности и корректности

Постоянный обзор того, что приходит в `main`: локальный сервер IDE, песочница
файлов модели, разбор чужих файлов, вывод в SVG/JSON, сборки. Модель угроз —
чужая веб-страница в браузере пользователя и чужой проект (`.spj`, `.cls`,
`.vdr`, `.stt`, `project.json`), открытый пользователем.

Серьёзность: **высокая** — чужой файл или страница пишет/читает вне
разрешённого или роняет IDE; **средняя** — отказ в обслуживании, обход
ограничения при особых условиях; **низкая** — надёжность, защита в глубину.

## 2026-09-25 — 2026-09-26

### Что проверено

- `core/src/player/guard.rs`, `mod.rs`: токен (128 бит, сравнение без раннего
  выхода, cookie `HttpOnly; SameSite=Strict`), `Host` против DNS rebinding,
  `Origin` (включая `null`), только POST для изменений, пределы заголовков,
  тела и соединений. Все маршруты `api.rs` и `editor.rs` за токеном; `GET` не
  меняют состояние.
- Вывод модели в IDE: `gfx/svg.rs` экранирует тексты, имена, шрифты, классы
  контролов; цвета и числа — только форматированные числа; JSON — через
  `json_string`. В `app/src`: `insertAdjacentHTML`/`innerHTML` получают только
  SVG ядра; заголовки окон, подписи контролов, диалоги модели, гиперссылки —
  через `textContent` и React. CSP страницы IDE и `tauri.conf.json` — без
  `unsafe-eval` и чужих скриптов.
- Гипербаза (86d0c4f): «запуск приложения» не выполняется — только сообщение;
  «загрузка проекта» — после подтверждения пользователя с показом пути. Поиск
  файлов модели (`find_file`) берёт только имя файла, без пути.
- Песочница файлов модели (`runtime/extra.rs`): `CreateStream`, `FileCopy`,
  `FileRename`, `FileDelete`, `CreateDir`, `DeleteDir`, `GetFileList`,
  `MSaveAs`, `Save/LoadObjectState` — все идут через `sandboxed`. Процессы ядро
  не запускает нигде.
- Разборщики `.cls`, `.vdr`, `.stt`, `.spj`, `.dbm/.bmp`: новый тест
  `core/tests/fuzz_parsers.rs` — обрезки и порченые копии файлов корпуса
  (`STRATUM_FUZZ_SCALE=30` — долгий прогон, ~50 тыс. вариантов), вложенность
  текста имиджа и `project.json`, циклические группы рисунка.
- `unsafe` в `runtime/clock.rs`: раскладка `TIME_ZONE_INFORMATION` верна,
  разбор TZif — через `get`, без индексации за пределы.
- GitHub Actions: в `run:` нет подстановок недоверенных данных (метка
  релиза — через `$GITHUB_REF_NAME`); права токена — см. ниже.
- `npm audit --omit=dev` в `app/`.

### Находки

| # | Серьёзность | Что | Статус |
| --- | --- | --- | --- |
| 1 | высокая | Символьная ссылка в папке проекта (например, из распакованного архива) выводила запись и чтение модели за пределы песочницы: проверка была только по записи пути | исправлено: «Песочница файлов модели: ссылки, корни и «C:» перед кириллицей» |
| 2 | высокая | `DeleteDir("")` / `DeleteDir(".")` стирал всю папку проекта, `DeleteDir` временной папки — все файлы пользователя в ней; `FileRename` уносил папку проекта целиком | исправлено тем же коммитом |
| 3 | высокая | Текст имиджа из чужого `.cls` с глубокой вложенностью («((((…», «----…») переполнял стек разбора — процесс IDE падал при открытии проекта | исправлено: «Текст имиджа: предел вложенности вместо переполнения стека» |
| 4 | высокая | `.vdr` с размером чанка меньше заголовка: разбор зацикливался и копил объекты до исчерпания памяти (найдено тестом на порченых файлах) | исправлено: «Разбор испорченных файлов: без паник и зацикливания» |
| 5 | средняя | BMP с заголовком за концом файла — паника; огромные размеры без данных — попытка выделить гигабайты | исправлено тем же коммитом |
| 6 | средняя | `.vdr`: «BM» у самого конца файла — чтение за пределами (паника) | исправлено тем же коммитом |
| 7 | средняя | Паника под общим замком (ядро на данных модели) отравляла `Mutex` — дальше каждый запрос и такт падали, IDE переставала работать до перезапуска; место соединения при панике не освобождалось, после 64 паник сервер отвечал только 503 | исправлено: «Локальный сервер: тело после проверок, паника не роняет IDE» |
| 8 | средняя | Тело запроса (до 32 МБ) читалось до проверки `Host`/`Origin`/токена: любая страница в браузере могла занимать память и все 64 соединения | исправлено тем же коммитом |
| 9 | средняя | `"C:Ж…"` в пути модели: срез строки по байту 3 разрезал букву — паника в потоке симуляции | исправлено: коммит песочницы (№ 1) |
| 10 | низкая | `url_decode`: «%» перед не-ASCII — паника (только с токеном) | исправлено: коммит сервера (№ 7) |
| 11 | низкая | `project.json`: «[[[[…» — переполнение стека; «\uD800» с не-низкой второй половиной — вычитание в минус | исправлено: «project.json: предел вложенности и проверка суррогатной пары» |
| 12 | низкая | Файлы сборки IDE: путь `/C:/…` на Windows превращался в абсолютный (`dir.join`) — чтение любого файла без токена; ответ чужой странице недоступен (SOP), но это обход | исправлено: коммит сервера (№ 7) |
| 13 | низкая | GitHub Actions: права `GITHUB_TOKEN` по умолчанию (могут быть на запись) | исправлено: «Сборки: токен GitHub Actions только на чтение» |
| 14 | низкая | `npm audit`: `dompurify` 3.4.8 в `monaco-editor` 0.56 (4 совета, moderate) | оставлено: исправление — `monaco-editor` 0.57, ломающее обновление; `dompurify` используется Monaco только для его собственной разметки (подсказки), модельные строки туда не идут. Обновить при следующем обновлении Monaco |
| 15 | низкая | Циклические группы в `.vdr` (группа в самой себе) | не подтвердилось: рендер и габариты это выдерживают (тест `cyclic_groups_do_not_recurse_forever`) |
| 17 | высокая | Чужой `project.json` задавал `"libraries": ["/"]` (или `../..`) — папки библиотек расширяют песочницу модели на чтение, и модель читала любой файл на диске; `"file": "../../…"` имиджа читал файлы вне проекта | исправлено: «project.json: библиотеки и файлы имиджей — только внутри проекта» |
| 18 | низкая | Редактор растров (`/api/picture`, `bitmap`/`dibset`): w×h×3 переполнял u32, не-hex или не-ASCII строка пикселей — паника (только из IDE с токеном) | исправлено: «Редактор растров и матрицы модели: размеры без переполнения» |
| 19 | низкая | Матрицы модели: границы `MCreate` около пределов i64 — переполнение разности и произведения (паника или пустая матрица) | исправлено тем же коммитом |
| 16 | низкая | `with_wrapper` (c6fe255) и загрузка рисунка ищут объекты верхнего уровня за O(n²) — рисунок из сотен тысяч объектов грузится долго | оставлено: не зависание, на настоящих файлах незаметно |

### Замечания без исправления

- Песочница по-прежнему разрешает запись во всю папку проекта: если
  пользователь открыл `.spj`, лежащий прямо в домашней папке, модель может
  писать в неё. Это решение ТЗ (§10), не ошибка.
- `POST /api/vdr/save`, `/api/state`, `/api/new`, `/api/open` пишут и читают
  по любому пути: они доступны только странице IDE с токеном и вызываются
  действием пользователя (диалог выбора файла).
